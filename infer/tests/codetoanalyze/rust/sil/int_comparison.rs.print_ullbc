# Final ULLBC before serialization:

// Full name: int_comparison::compare
pub fn compare(@1: i32, @2: i32) -> i32
{
    let @0: i32; // return
    let x@1: i32; // arg #1
    let y@2: i32; // arg #2
    let @3: bool; // anonymous local
    let @4: i32; // anonymous local
    let @5: i32; // anonymous local

    bb0: {
        storage_live(@3);
        storage_live(@4);
        @4 := copy (x@1);
        storage_live(@5);
        @5 := copy (y@2);
        @3 := move (@4) > move (@5);
        if move (@3) -> bb1 else -> bb2;
    }

    bb1: {
        storage_dead(@5);
        storage_dead(@4);
        @0 := copy (x@1);
        goto bb3;
    }

    bb2: {
        storage_dead(@5);
        storage_dead(@4);
        @0 := copy (y@2);
        goto bb3;
    }

    bb3: {
        storage_dead(@3);
        return;
    }
}

// Full name: int_comparison::main
fn main()
{
    let @0: (); // return
    let x@1: i32; // local
    let y@2: i32; // local
    let @3: i32; // anonymous local
    let @4: i32; // anonymous local
    let @5: i32; // anonymous local

    bb0: {
        storage_live(x@1);
        x@1 := const (0 : i32);
        storage_live(y@2);
        y@2 := const (1 : i32);
        storage_live(@3);
        storage_live(@4);
        @4 := copy (x@1);
        storage_live(@5);
        @5 := copy (y@2);
        @3 := compare(move (@4), move (@5)) -> bb1 (unwind: bb2);
    }

    bb1: {
        storage_dead(@5);
        storage_dead(@4);
        storage_dead(@3);
        @0 := ();
        storage_dead(y@2);
        storage_dead(x@1);
        @0 := ();
        return;
    }

    bb2: {
        unwind_continue;
    }
}



