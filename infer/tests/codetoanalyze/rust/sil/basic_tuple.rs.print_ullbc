# Final ULLBC before serialization:

// Full name: basic_tuple::main
fn main()
{
    let @0: (); // return
    let x@1: (i32, i32); // local
    let one@2: i32; // local
    let two@3: i32; // local
    let @4: i32; // anonymous local
    let @5: i32; // anonymous local

    bb0: {
        storage_live(x@1);
        x@1 := (const (1 : i32), const (2 : i32));
        storage_live(one@2);
        one@2 := copy ((x@1).0);
        storage_live(two@3);
        two@3 := copy ((x@1).1);
        storage_live(@4);
        @4 := copy (two@3);
        (x@1).0 := move (@4);
        storage_dead(@4);
        storage_live(@5);
        @5 := copy (one@2);
        (x@1).1 := move (@5);
        storage_dead(@5);
        @0 := ();
        storage_dead(two@3);
        storage_dead(one@2);
        storage_dead(x@1);
        @0 := ();
        return;
    }
}



