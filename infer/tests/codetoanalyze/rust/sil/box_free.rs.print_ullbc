# Final ULLBC before serialization:

// Full name: core::ops::drop::Drop
#[lang_item("drop")]
pub trait Drop<Self>
{
    fn drop<'_0> = core::ops::drop::Drop::drop<'_0_0, Self>[Self]
    vtable: core::ops::drop::Drop::{vtable}
}

pub fn core::ops::drop::Drop::drop<'_0, Self>(@1: &'_0 mut (Self))
where
    [@TraitClause0]: Drop<Self>,

// Full name: alloc::alloc::Global
#[lang_item("global_alloc_ty")]
pub struct Global {}

// Full name: alloc::boxed::{impl Drop for alloc::boxed::Box<T>}::drop
pub fn {impl Drop for alloc::boxed::Box<T>}::drop<'_0, T, A>(@1: &'_0 mut (alloc::boxed::Box<T>))

// Full name: alloc::boxed::{impl Drop for alloc::boxed::Box<T>}
impl<T, A> Drop for alloc::boxed::Box<T> {
    fn drop<'_0> = {impl Drop for alloc::boxed::Box<T>}::drop<'_0_0, T, A>
    vtable: {impl Drop for alloc::boxed::Box<T>}::{vtable}<T, A>
}

// Full name: box_free::main
fn main()
{
    let @0: (); // return
    let ptr@1: *const i32; // local
    let @2: (); // anonymous local
    let x@3: alloc::boxed::Box<i32>; // local
    let @4: *const i32; // anonymous local
    let @5: &'_ (i32); // anonymous local
    let ub@6: i32; // local

    bb0: {
        storage_live(ptr@1);
        storage_live(@2);
        storage_live(x@3);
        x@3 := @BoxNew<i32>(const (50 : i32)) -> bb1 (unwind: bb2);
    }

    bb1: {
        storage_live(@4);
        storage_live(@5);
        @5 := &*(x@3);
        @4 := &raw const *(@5);
        ptr@1 := move (@4);
        storage_dead(@4);
        storage_dead(@5);
        // Box is freed here since borrowchecker does not keep track of raw pointer
        drop[{impl Drop for alloc::boxed::Box<T>}<i32, Global>] x@3;
        storage_dead(x@3);
        storage_dead(@2);
        storage_live(ub@6);
        ub@6 := copy (*(ptr@1));
        @0 := ();
        storage_dead(ub@6);
        storage_dead(ptr@1);
        @0 := ();
        return;
    }

    bb2: {
        unwind_continue;
    }
}



