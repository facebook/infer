# Final ULLBC before serialization:

// Full name: nested_type::Foo
struct Foo {
  array: Array<i32, 2 : usize>,
  tuple: (i32, i32),
}

// Full name: nested_type::main
fn main()
{
    let @0: (); // return
    let x@1: Foo; // local
    let @2: Array<i32, 2 : usize>; // anonymous local
    let @3: (i32, i32); // anonymous local

    bb0: {
        storage_live(x@1);
        storage_live(@2);
        @2 := [const (1 : i32), const (2 : i32)];
        storage_live(@3);
        @3 := (const (3 : i32), const (4 : i32));
        x@1 := Foo { array: move (@2), tuple: move (@3) };
        storage_dead(@3);
        storage_dead(@2);
        @0 := ();
        storage_dead(x@1);
        @0 := ();
        return;
    }
}



