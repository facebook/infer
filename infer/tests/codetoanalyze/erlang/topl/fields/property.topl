property Field
  start -> start: *
  start -> error: "topl_fields:bar"(T, R) when 11 <= (T:Cons.head):Integer.value

property NoOne
  start -> start: *
  start -> error: "topl_fields:dontSayOne"(A, R) when A:Integer.value == 1
