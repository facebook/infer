.source_language = "hack"

type T = { }

define object() : void {
  #entry:
    n0 = __sil_allocate(<T>)
    ret null
}

define array() : void {
  #entry:
    n0 = __sil_allocate_array(<int>,10)
    ret null
}
