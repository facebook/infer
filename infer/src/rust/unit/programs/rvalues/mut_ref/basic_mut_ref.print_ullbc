# Final ULLBC before control-flow reconstruction:

// Full name: basic_mut_ref::main
fn main()
{
    let @0: (); // return
    let y@1: i32; // local
    let x@2: &'_ mut (i32); // local
    let @3: (); // anonymous local

    bb0: {
        storage_live(y@1);
        y@1 := const (10 : i32);
        storage_live(x@2);
        x@2 := &mut y@1;
        nop;
        @0 := ();
        storage_dead(x@2);
        storage_dead(y@1);
        @0 := ();
        return;
    }
}