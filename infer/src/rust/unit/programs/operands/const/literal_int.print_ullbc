# Final ULLBC before control-flow reconstruction:

// Full name: literal_int::main
fn main()
{
    let @0: (); // return
    let @1: i32; // anonymous local
    let @2: (); // anonymous local

    bb0: {
        storage_live(@1);
        @1 := const (42 : i32);
        storage_dead(@1);
        nop;
        @0 := ();
        @0 := ();
        return;
    }
}