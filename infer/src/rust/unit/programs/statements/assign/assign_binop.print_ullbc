# Final ULLBC before control-flow reconstruction:

// Full name: assign_binop::main
fn main()
{
    let @0: (); // return
    let x@1: i32; // local
    let @2: i32; // anonymous local
    let @3: (); // anonymous local

    bb0: {
        storage_live(x@1);
        @2 := const (2 : i32) panic.+ const (3 : i32);
        nop;
        x@1 := move (@2);
        nop;
        @0 := ();
        storage_dead(x@1);
        @0 := ();
        return;
    }
}