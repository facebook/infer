# Final ULLBC before control-flow reconstruction:

// Full name: assign_tuple::main
fn main()
{
    let @0: (); // return
    let x@1: (i32, i32); // local
    let @2: (); // anonymous local

    bb0: {
        storage_live(x@1);
        x@1 := (const (1 : i32), const (2 : i32));
        nop;
        @0 := ();
        storage_dead(x@1);
        @0 := ();
        return;
    }
}